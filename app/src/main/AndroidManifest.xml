<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.base.createbase">

    <application
        android:name=".AppCreateBase"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Splash" >
        <activity android:name=".SplashActivity"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.HomeActivity" android:theme="@style/Theme.Splash" />
        <activity android:name=".ui.NavigationActivity" android:theme="@style/Theme.CreateBase" />
        <activity android:name=".ui.TabHostActivity" android:theme="@style/Theme.Splash" />
        <activity android:name=".ui.ViewPageActivity" android:theme="@style/Theme.Splash" />
    </application>

</manifest>